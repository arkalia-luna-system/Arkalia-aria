name: 📚 Deploy Documentation - GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - ".github/workflows/deploy-docs.yml"
  pull_request:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - ".github/workflows/deploy-docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "deploy-docs-${{ github.ref }}"
  cancel-in-progress: true

# Workflow indépendant du CI/CD - peut s'exécuter en parallèle
# Note: Ce workflow a une priorité plus basse et ne bloque pas le CI/CD

jobs:
  build-docs:
    name: 📚 Build Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: "pip"

      - name: 📦 Install Documentation Dependencies
        timeout-minutes: 5
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs mkdocs-material mkdocs-mermaid2-plugin mkdocs-git-revision-date-plugin

      - name: 📚 Build Documentation
        timeout-minutes: 10
        continue-on-error: true
        run: |
          echo "📚 Building documentation with MkDocs..."
          mkdocs build --clean --strict || (echo "⚠️ Documentation build completed with warnings" && exit 0)

      - name: 🧹 Nettoyer répertoire site (supprimer tous les liens)
        if: always()
        run: |
          if [ -d "site" ]; then
            echo "🧹 Nettoyage approfondi du répertoire site..."
            echo "📊 Taille initiale:"
            du -sh site
            
            # Créer une copie complète sans liens symboliques ni physiques
            echo "📦 Création d'une copie propre sans liens..."
            rm -rf site_clean
            mkdir -p site_clean
            
            # Copier tous les fichiers en suivant les liens symboliques (convertir en copies)
            # Utiliser cp -rL pour suivre les liens symboliques et les convertir
            cp -rL site/* site_clean/ 2>/dev/null || cp -r site/* site_clean/ 2>/dev/null || true
            cp -rL site/.[!.]* site_clean/ 2>/dev/null || true
            
            # Supprimer l'ancien répertoire et renommer le nouveau
            rm -rf site
            mv site_clean site
            
            # Vérification finale : supprimer tout lien restant
            find site -type l -delete 2>/dev/null || true
            
            # Vérifier qu'il ne reste plus de liens
            SYMLINKS=$(find site -type l 2>/dev/null | wc -l)
            HARD_LINKS=$(find site -type f -links +1 2>/dev/null | wc -l)
            
            if [ "$SYMLINKS" -gt 0 ]; then
              echo "⚠️ Attention: $SYMLINKS liens symboliques restants - suppression forcée..."
              find site -type l -delete
            fi
            
            if [ "$HARD_LINKS" -gt 0 ]; then
              echo "⚠️ Attention: $HARD_LINKS fichiers avec liens physiques - conversion..."
              find site -type f -links +1 -exec sh -c 'cp "$1" "$1.tmp" && mv "$1.tmp" "$1"' _ {} \;
            fi
            
            # Vérifier la taille
            SIZE=$(du -sh site | cut -f1)
            SIZE_BYTES=$(du -sb site | cut -f1)
            echo "📊 Taille finale: $SIZE ($SIZE_BYTES bytes)"
            
            if [ "$SIZE_BYTES" -gt 1073741824 ]; then
              echo "⚠️ Attention: Taille supérieure à 1GB ($SIZE_BYTES bytes)"
            fi
            
            # Vérification finale
            FINAL_SYMLINKS=$(find site -type l 2>/dev/null | wc -l)
            FINAL_HARD_LINKS=$(find site -type f -links +1 2>/dev/null | wc -l)
            
            if [ "$FINAL_SYMLINKS" -eq 0 ] && [ "$FINAL_HARD_LINKS" -eq 0 ]; then
              echo "✅ Nettoyage terminé - Aucun lien restant"
            else
              echo "⚠️ Liens restants: $FINAL_SYMLINKS symboliques, $FINAL_HARD_LINKS physiques"
            fi
          fi

      - name: 📤 Upload GitHub Pages artifact
        if: always()
        continue-on-error: true
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/

  deploy-docs:
    name: 🚀 Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: [build-docs]
    if: always() && ((github.ref == 'refs/heads/main' && github.event_name == 'push') || github.event_name == 'workflow_dispatch')
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: ⚠️ Vérification GitHub Pages
        run: |
          echo "⚠️ Si cette étape échoue avec une erreur 404, GitHub Pages n'est pas activé."
          echo "📚 Veuillez activer GitHub Pages dans les paramètres du repository :"
          echo "   https://github.com/arkalia-luna-system/Arkalia-aria/settings/pages"
          echo "   → Source: 'GitHub Actions'"
          echo ""

      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        timeout-minutes: 5
        uses: actions/deploy-pages@v4

      - name: 📢 Résultat du déploiement
        if: failure()
        run: |
          echo "❌ Échec du déploiement GitHub Pages"
          echo ""
          echo "🔧 SOLUTION :"
          echo "1. Aller sur : https://github.com/arkalia-luna-system/Arkalia-aria/settings/pages"
          echo "2. Dans 'Source', sélectionner 'GitHub Actions'"
          echo "3. Cliquer sur 'Save'"
          echo "4. Relancer le workflow après activation"
          echo ""
          echo "📖 Guide complet : docs/GITHUB_PAGES_SETUP.md"
          exit 1

  notify-deployment:
    name: 📢 Notify Deployment
    runs-on: ubuntu-latest
    needs: deploy-docs
    if: always()
    timeout-minutes: 5
    steps:
      - name: 📢 Deployment Status
        run: |
          if [[ "${{ needs.deploy-docs.result }}" == "success" ]]; then
            echo "✅ Documentation deployed successfully!"
            echo "📚 Documentation has been deployed to GitHub Pages"
          else
            echo "❌ Documentation deployment failed!"
          fi
