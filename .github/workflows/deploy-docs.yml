name: 📚 Deploy Documentation - GitHub Pages

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "deploy-docs-${{ github.ref }}"
  cancel-in-progress: true

# Workflow indépendant du CI/CD - peut s'exécuter en parallèle
# Note: Ce workflow a une priorité plus basse et ne bloque pas le CI/CD

jobs:
  build-docs:
    name: 📚 Build Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: "pip"

      - name: 📦 Install Documentation Dependencies
        timeout-minutes: 5
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs mkdocs-material mkdocs-mermaid2-plugin mkdocs-git-revision-date-plugin

      - name: 📚 Build Documentation
        timeout-minutes: 10
        continue-on-error: true
        run: |
          echo "📚 Building documentation with MkDocs..."
          mkdocs build --clean --strict || (echo "⚠️ Documentation build completed with warnings" && exit 0)

      - name: 🧹 Nettoyer répertoire site
        if: always()
        run: |
          if [ -d "site" ]; then
            echo "🧹 Nettoyage du répertoire site..."
            # Supprimer les liens symboliques
            find site -type l -delete
            # Supprimer les fichiers cachés
            find site -name ".*" -type f -delete
            find site -name ".*" -type d -exec rm -rf {} + 2>/dev/null || true
            # Vérifier la taille
            du -sh site
            echo "✅ Nettoyage terminé"
          fi

      - name: 📤 Upload GitHub Pages artifact
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: github-pages
          path: site/
          retention-days: 1
          if-no-files-found: warn

  deploy-docs:
    name: 🚀 Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: [build-docs]
    if: always() && github.ref == 'refs/heads/main' && github.event_name == 'push'
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 📥 Download artifact
        uses: actions/download-artifact@v4
        with:
          name: github-pages
          path: ./

      - name: ⚠️ Vérification GitHub Pages
        run: |
          echo "⚠️ Si cette étape échoue avec une erreur 404, GitHub Pages n'est pas activé."
          echo "📚 Veuillez activer GitHub Pages dans les paramètres du repository :"
          echo "   https://github.com/arkalia-luna-system/Arkalia-aria/settings/pages"
          echo "   → Source: 'GitHub Actions'"
          echo ""

      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        timeout-minutes: 5
        uses: actions/deploy-pages@v4

      - name: 📢 Résultat du déploiement
        if: failure()
        run: |
          echo "❌ Échec du déploiement GitHub Pages"
          echo ""
          echo "🔧 SOLUTION :"
          echo "1. Aller sur : https://github.com/arkalia-luna-system/Arkalia-aria/settings/pages"
          echo "2. Dans 'Source', sélectionner 'GitHub Actions'"
          echo "3. Cliquer sur 'Save'"
          echo "4. Relancer le workflow après activation"
          echo ""
          echo "📖 Guide complet : docs/GITHUB_PAGES_SETUP.md"
          exit 1

  notify-deployment:
    name: 📢 Notify Deployment
    runs-on: ubuntu-latest
    needs: deploy-docs
    if: always()
    timeout-minutes: 5
    steps:
      - name: 📢 Deployment Status
        run: |
          if [[ "${{ needs.deploy-docs.result }}" == "success" ]]; then
            echo "✅ Documentation deployed successfully!"
            echo "📚 Documentation has been deployed to GitHub Pages"
          else
            echo "❌ Documentation deployment failed!"
          fi
